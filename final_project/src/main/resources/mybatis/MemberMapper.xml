<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hk.project.mapper.MemberMapper">
	
		<insert id="addUser" parameterType="memberDto">
		INSERT INTO MEMBER
		VALUES(null,#{id},#{password},#{name},#{email},#{address},#{role},#{useraccesstoken}, #{userrefreshtoken
		},#{userseqno}, #{phone} )
	</insert>
	
	<select id="idChk" parameterType="String" resultType="String">
		SELECT ID FROM MEMBER WHERE ID = #{id}
	</select>
	
	<select id="loginUser" parameterType="String" resultType="memberDto">
		SELECT MEMBERID, ID, NAME, PASSWORD, EMAIL, ADDRESS, ROLE
		FROM MEMBER
		WHERE ID = #{id}
	</select>
	
	<select id="getUser" parameterType="String" resultType="memberDto">
      SELECT MEMBERID, ID, NAME, PASSWORD, EMAIL, ADDRESS, ROLE, PHONE
      FROM MEMBER
      WHERE ID = #{id}
   </select>
   
    <update id="userUpdate" parameterType="memberDto" >
     UPDATE MEMBER
	 SET PASSWORD = #{password}
	 WHERE ID = #{id}
    </update>
</mapper>
















